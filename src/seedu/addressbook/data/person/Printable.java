package seedu.addressbook.data.person;

/**
 * Represents a printable entity for all data in the addressBook.
 * Implementations should guarantee: public String getPrintableString() methods must exist and must return a String.
 */
public interface Printable {

    String getPrintableString();
}
