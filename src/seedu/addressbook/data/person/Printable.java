package seedu.addressbook.data.person;

/**
 * Interface for printable descriptors of a Person in the addressbook.
 */
public interface Printable {
    /**
     * @return a printable strings of an object.
     */
    String getPrintableString();
}
